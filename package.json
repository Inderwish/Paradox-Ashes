{
  "name": "paradox-ashes",
  "version": "0.1.2-alpha",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.js --bundle --format=iife --platform=browser --target=es2020 --minify --outfile=dist/app.bundle.js"
  },
  "dependencies": {
    "three": "0.169.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
